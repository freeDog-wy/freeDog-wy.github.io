<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>C语言中那些奇奇怪怪的宏</title>
    <url>/2024/04/14/C%E8%AF%AD%E8%A8%80%E4%B8%AD%E9%82%A3%E4%BA%9B%E5%A5%87%E5%A5%87%E6%80%AA%E6%80%AA%E7%9A%84%E5%AE%8F/</url>
    <content><![CDATA[<p>今天整理了一下C语言一些奇奇怪怪的宏，给大家瞧一瞧。</p>
<span id="more"></span>

<h2 id="TO-STR-添加双引号"><a href="#TO-STR-添加双引号" class="headerlink" title="TO_STR 添加双引号"></a>TO_STR 添加双引号</h2><p>通过在一个标识符前添加 # 号，可以给这个标识符加上双引号。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> TO_STR(x) #x</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">printf</span>(<span class="string">&quot;%s\n&quot;</span>, TO_STR(hello world));</span><br><span class="line"><span class="built_in">printf</span>(<span class="string">&quot;%s\n&quot;</span>, TO_STR(<span class="number">234</span>));</span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">hello world</span><br><span class="line">234</span><br></pre></td></tr></table></figure>

<h2 id="TO-CHAR-添加单引号"><a href="#TO-CHAR-添加单引号" class="headerlink" title="TO_CHAR 添加单引号"></a>TO_CHAR 添加单引号</h2><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> TO_CHAR(x) #@x</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">printf</span>(<span class="string">&quot;%c\n&quot;</span>, TO_CHAR(a));</span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">a</span><br></pre></td></tr></table></figure>
<p><code>a</code>并不是变量，啥也不是，只是通过预处理器给它套了一个单引号，它就变成字符了。</p>
<h2 id="PRIMITIVE-CAT-字符拼接"><a href="#PRIMITIVE-CAT-字符拼接" class="headerlink" title="PRIMITIVE_CAT 字符拼接"></a>PRIMITIVE_CAT 字符拼接</h2><p>符号 ## 可以拼接两个标识符。<br>至于为什么这里给它取名叫PRIMITIVE_CAT宏，是因为宏扩展的一个机制，往下看就知道了。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> PRIMITIVE_CAT(x,y)   x ## y</span></span><br><span class="line"><span class="type">int</span> age = <span class="number">10</span>;</span><br><span class="line"></span><br><span class="line"><span class="built_in">printf</span>(<span class="string">&quot;age: %d\n&quot;</span>, PRIMITIVE_CAT(a, ge));</span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">age: 10</span><br></pre></td></tr></table></figure>

<h2 id="CAT-字符拼接"><a href="#CAT-字符拼接" class="headerlink" title="CAT 字符拼接"></a>CAT 字符拼接</h2><p>可能你会感到奇怪，这不就是套了一层皮吗，有啥区别呢？</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> CAT(x,y) PRIMITIVE_CAT(x,y)</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> X a</span></span><br><span class="line"><span class="type">int</span> age = <span class="number">10</span>;</span><br><span class="line"><span class="built_in">printf</span>(<span class="string">&quot;%d\n&quot;</span>, CAT(X, ge));</span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">10</span><br></pre></td></tr></table></figure>
<p>但是如果把上面的 <code>CAT</code> 替换成 <code>PRIMITIVE_CAT</code>，看看会发生啥？</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> CAT(a,b) PRIMITIVE_CAT(a,b)</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> X a</span></span><br><span class="line"><span class="type">int</span> age = <span class="number">10</span>;</span><br><span class="line"><span class="built_in">printf</span>(<span class="string">&quot;%d\n&quot;</span>, PRIMITIVE_CAT(X, ge));</span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">找不到变量 Xge</span><br></pre></td></tr></table></figure>
<p><strong>报错了</strong><br>预处理器直接把 <code>X</code> 拿来拼接了，并没有把 <code>X</code> 替换成 <code>a</code>.<br>那原因是什么呢？</p>
<p>首先宏函数展开有两种情况：</p>
<ol>
<li>参数参与了 ## 或者 # 符号的操作<br> 此时，由于 <code>PRIMITIVE_CAT</code> 宏函数中的参数<code>X Y</code>参与了## 拼接，那么就不会对宏 <code>X Y</code> 进行展开后传入，而是直接传入。 <figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> X 0</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> Y 1</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> XY 2</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> PRIMITIVE_CAT(a,b) a##b</span></span><br><span class="line"><span class="built_in">printf</span>(<span class="string">&quot;%d\n&quot;</span>, PRIMITIVE_CAT(X,Y));</span><br></pre></td></tr></table></figure>
 所以拼接结果是 <code>PRIMITIVE_CAT(X,Y) -&gt; XY -&gt; 2</code>.<br> 拼接后得到了宏<code>XY</code>，直接展开成 <code>2</code>. <figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">2</span><br></pre></td></tr></table></figure></li>
<li>参数没有参与 ## 或者 # 符号的操作<br> 此时，<code>CAT</code> 宏函数的参数并没有参与拼接，则先对参数 <code>X Y</code>进行展开，分别展开为 <code>0 1</code>，然后传入宏函数。 <figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> X 0</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> Y 1</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> CAT(a,b) PRIMITIVE_CAT(a,b)</span></span><br><span class="line"><span class="built_in">printf</span>(<span class="string">&quot;%d\n&quot;</span>, CAT(X,Y));</span><br></pre></td></tr></table></figure>
 所以拼接结果是<code>CAT(X,Y) -&gt; PRIMITIVE_CAT(0,1) -&gt; 01</code> <figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">1</span><br></pre></td></tr></table></figure>
可以看出这两个拼接宏函数，都是很常用的。</li>
</ol>
<h2 id="OFFSET-OF-得到一个字段在结构体struct中的偏移量-字节数"><a href="#OFFSET-OF-得到一个字段在结构体struct中的偏移量-字节数" class="headerlink" title="OFFSET_OF 得到一个字段在结构体struct中的偏移量(字节数)"></a>OFFSET_OF 得到一个字段在结构体struct中的偏移量(字节数)</h2><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> OFFSET_OF(type,field) ( (size_t) &amp;(( type *) 0)-&gt; field )</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> NAME_SIZE 20</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Person</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="type">char</span> name[NAME_SIZE];</span><br><span class="line">    <span class="type">int</span> score;</span><br><span class="line">    <span class="type">int</span> age;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;age offset of struct Person: %u\n&quot;</span>, OFFSET_OF(<span class="keyword">struct</span> Person, age));</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">24</span><br></pre></td></tr></table></figure>
<p>在我的电脑上，<code>int</code>类型是<code>4</code>字节的，<code>char</code>类型是<code>1</code>字节的。所以字段 <code>age</code> 排在 <code>name</code> 和 <code>score</code>的后面，他到结构体首地址偏移就是 <code>24</code>字节。</p>
<p>什么？你要问这个宏函数原理是啥？<br>这个很简单的，首先上面那个宏函数展开的结果是这个：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">(<span class="type">size_t</span>) &amp;((<span class="keyword">struct</span> Person *)<span class="number">0</span>)-&gt;age</span><br></pre></td></tr></table></figure>
<p>他把地址为<code>0</code>，强转为一个<code>struct Person *</code>类型，所以它变成了一个指针，然后就可以访问结构体中的 <code>age</code>字段了，再然后通过取地址符 <code>&amp;</code> 得到<code>age</code>字段的地址，很显然，因为结构体首地址是<code>0</code>，所以<code>age</code>字段的地址就是偏移量啦。</p>
<h2 id="FILED-SIZE-得到一个结构体中字段的大小（字节数）"><a href="#FILED-SIZE-得到一个结构体中字段的大小（字节数）" class="headerlink" title="FILED_SIZE 得到一个结构体中字段的大小（字节数）"></a>FILED_SIZE 得到一个结构体中字段的大小（字节数）</h2><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> FILED_SIZE( type, field ) sizeof( ((type *) 0)-&gt;field )</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">printf</span>(<span class="string">&quot;filed size of age: %u\n&quot;</span>, FILED_SIZE(<span class="keyword">struct</span> Person, age));</span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">4</span><br></pre></td></tr></table></figure>
<p>在我的电脑上<code>int</code>类型<code>4</code>个字节.</p>
<h2 id="CONTAINER-OF-根据结构体变量字段地址获取整个结构体的存储空间首地址"><a href="#CONTAINER-OF-根据结构体变量字段地址获取整个结构体的存储空间首地址" class="headerlink" title="CONTAINER_OF 根据结构体变量字段地址获取整个结构体的存储空间首地址"></a>CONTAINER_OF 根据结构体变量字段地址获取整个结构体的存储空间首地址</h2><ul>
<li><code>ptr</code>：结构体字段的指针</li>
<li><code>type</code>：结构体类型</li>
<li><code>filed</code>: 字段名称<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> CONTAINER_OF(ptr, type, filed) \</span></span><br><span class="line"><span class="meta">    (type *)((char *)(ptr) - (char *) &amp;((type *)0)-&gt;filed )</span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">Person</span> <span class="title">p</span> =</span> &#123;</span><br><span class="line">        .name = <span class="string">&quot;dog&quot;</span>,</span><br><span class="line">        .score = <span class="number">30</span>,</span><br><span class="line">        .age = <span class="number">2332432</span></span><br><span class="line">    &#125;;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">Person</span> *<span class="title">pp</span> =</span> CONTAINER_OF(&amp;p.age, <span class="keyword">struct</span> Person, age);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;name: %s\n&quot;</span>, pp-&gt;name);</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">dog</span><br></pre></td></tr></table></figure>
虽然这个样例看起来多此一举，但是这个宏函数在操作系统底层用是很常用的。<br>可以用于C的面向对象设计，类的继承，从父类访问子类。</li>
</ul>
<p>原理是啥？<br>其实只要理解了C语言的内存布局，这个就不是问题。</p>
<h2 id="UPPERCASE-字母大小写转换"><a href="#UPPERCASE-字母大小写转换" class="headerlink" title="UPPERCASE 字母大小写转换"></a>UPPERCASE 字母大小写转换</h2><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> UPPERCASE(c) ( ((c) &gt;= <span class="string">&#x27;a&#x27;</span> &amp;&amp; (c) &lt;= <span class="string">&#x27;z&#x27;</span>) ? ((c) - 0x20) : (c) )</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> LOWERCASE(c) ( ((c) &gt;= <span class="string">&#x27;a&#x27;</span> &amp;&amp; (c) &lt;= <span class="string">&#x27;z&#x27;</span>) ? (c) : ((c) + 0x20))</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">printf</span>(<span class="string">&quot;Uppercase: %c -&gt; %c\n&quot;</span>, <span class="string">&#x27;x&#x27;</span>, UPPERCASE(<span class="string">&#x27;x&#x27;</span>));</span><br><span class="line"><span class="built_in">printf</span>(<span class="string">&quot;Lowercase: %c -&gt; %c\n&quot;</span>, <span class="string">&#x27;X&#x27;</span>, LOWERCASE(<span class="string">&#x27;X&#x27;</span>));</span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">Uppercase: x -&gt; X</span><br><span class="line">Lowercase: X -&gt; x</span><br></pre></td></tr></table></figure>


<h2 id="ARRAY-SIZE-获取一个数组元素的个数"><a href="#ARRAY-SIZE-获取一个数组元素的个数" class="headerlink" title="ARRAY_SIZE 获取一个数组元素的个数"></a>ARRAY_SIZE 获取一个数组元素的个数</h2><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> ARRAY_SIZE(arr) (sizeof((arr)) / sizeof((arr[0])))</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Person</span> <span class="title">p_arr</span>[10] =</span> &#123;<span class="number">0</span>&#125;;</span><br><span class="line"><span class="built_in">printf</span>(<span class="string">&quot;p_arr size: %u\n&quot;</span>, ARRAY_SIZE(p_arr));</span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">p_arr size: 10</span><br></pre></td></tr></table></figure>

<h2 id="LOG-打印日志"><a href="#LOG-打印日志" class="headerlink" title="LOG 打印日志"></a>LOG 打印日志</h2><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> LOG_ON 1</span></span><br><span class="line"><span class="meta">#<span class="keyword">if</span> LOG_ON</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> LOG(fmt, ...) \</span></span><br><span class="line"><span class="meta">    printf(<span class="string">&quot;[FILE: %s] [FUNCTION: %s] [LINE: %d] &quot;</span> fmt <span class="string">&quot;\n&quot;</span>, \</span></span><br><span class="line"><span class="meta">        __FILE__, __FUNCTION__, __LINE__, ##__VA_ARGS__)</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">else</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> LOG(fmt, ...)</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line"></span><br><span class="line">LOG(<span class="string">&quot;一次LOG测试&quot;</span>);</span><br><span class="line">LOG(<span class="string">&quot;测试可变参: %d&quot;</span>, <span class="number">4</span>);</span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">[FILE: D:\xx.c] [FUNCTION: main] [LINE: 72] 一次debug测试</span><br><span class="line">[FILE: D:\xx.c] [FUNCTION: main] [LINE: 73] 测试可变参: 4</span><br></pre></td></tr></table></figure>




<h2 id="FOR-EACH-函数宏递归实现For循环"><a href="#FOR-EACH-函数宏递归实现For循环" class="headerlink" title="FOR_EACH 函数宏递归实现For循环"></a>FOR_EACH 函数宏递归实现For循环</h2><ul>
<li><code>count</code>： 循环次数，我的这个实现最多只能循环三次，不过你可以继续增加</li>
<li><code>marco</code>： 相当于回调宏<ul>
<li><code>idx</code>： 当前枚举下标的，顺序是 <code>count, count - 1, count - 2, ..., 2, 1</code>, 倒序下标</li>
<li><code>x</code>：这个是枚举的值</li>
</ul>
</li>
<li><code>...</code>：可变参数，枚举的值<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> PRIMITIVE_CAT(a,b)   a ## b</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> CAT(a,b) PRIMITIVE_CAT(a,b)</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> INT_DEFINES(idx, x) int CAT(a_, x) = idx</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> PRINT(idx, x)    printf(#x<span class="string">&quot;: %d idx: %d\n&quot;</span>, CAT(a_, x), idx)</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> FOR_EACH(count, marco, ...) CAT(FOR_EACH_, count)(marco, ##__VA_ARGS__)</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> FOR_EACH_3(marco, arg, ...) marco(3, arg); FOR_EACH_2(marco, ##__VA_ARGS__)</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> FOR_EACH_2(marco, arg, ...) marco(2, arg); FOR_EACH_1(marco, ##__VA_ARGS__)</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> FOR_EACH_1(marco, arg, ...) marco(1, arg)</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// IF_ELSE (0) ( \</span></span><br><span class="line"><span class="comment">    //     printf(&quot;YES\n&quot;); \</span></span><br><span class="line"><span class="comment">    // )( \</span></span><br><span class="line"><span class="comment">    //     printf(&quot;NO\n&quot;); \</span></span><br><span class="line"><span class="comment">    // )</span></span><br><span class="line">    FOR_EACH(<span class="number">3</span>, INT_DEFINES, dog, cat, pig);</span><br><span class="line">    </span><br><span class="line">    FOR_EACH(<span class="number">3</span>, PRINT, dog, cat, pig);</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
宏展开后真实的样子是：<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> a_dog = <span class="number">3</span>; <span class="type">int</span> a_cat = <span class="number">2</span>; <span class="type">int</span> a_pig = <span class="number">1</span>;</span><br><span class="line"><span class="built_in">printf</span>(<span class="string">&quot;dog&quot;</span><span class="string">&quot;: %d idx: %d\n&quot;</span>, a_dog, <span class="number">3</span>); <span class="built_in">printf</span>(<span class="string">&quot;cat&quot;</span><span class="string">&quot;: %d idx: %d\n&quot;</span>, a_cat, <span class="number">2</span>); <span class="built_in">printf</span>(<span class="string">&quot;pig&quot;</span><span class="string">&quot;: %d idx: %d\n&quot;</span>, a_pig, <span class="number">1</span>);</span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">dog: 3 idx: 3</span><br><span class="line"><span class="built_in">cat</span>: 2 idx: 2</span><br><span class="line">pig: 1 idx: 1</span><br></pre></td></tr></table></figure>
不得不说，宏的代码可读性确实太差了，哈哈哈！<blockquote>
<p><strong>注意！！！</strong> 我用<code>MSVC</code>编译上述<code>FOR_EACH</code>宏代码出现了奇怪的问题： <code>x86</code> 的 <code>Microsoft (R) C/C++</code> 优化编译器 <code>19.39.33521</code> 版。<br>但是在 <code>GNU C</code>, <code>Clang</code> 上没有问题，大家也可以自己实验一下。</p>
</blockquote>
</li>
</ul>
]]></content>
      <categories>
        <category>编程语言</category>
        <category>C语言</category>
      </categories>
      <tags>
        <tag>C语言</tag>
        <tag>宏</tag>
      </tags>
  </entry>
  <entry>
    <title>C语言极简的双向链表实现</title>
    <url>/2024/04/18/C%E8%AF%AD%E8%A8%80%E6%9E%81%E7%AE%80%E7%9A%84%E5%8F%8C%E5%90%91%E9%93%BE%E8%A1%A8%E5%AE%9E%E7%8E%B0/</url>
    <content><![CDATA[<p>最近在阅读 <code>Fabrice Bellard</code> 巨佬的 <code>quickjs</code> 项目源码，阅读之前我还复习了现代的JavaScript的语法知识，以期望对阅读源码有所帮助。<br>先从最简单的开始，在 <code>quickjs</code> 的源码中有一个 <code>Fabrice Bellard</code> 实现的双向链表实现，整个实现就是一个头文件 <code>list.h</code>。</p>
<blockquote>
<p>在本文最下面提供了完整源码，希望对大家有所帮助。</p>
</blockquote>
<span id="more"></span>

<h2 id="链表结构体"><a href="#链表结构体" class="headerlink" title="链表结构体"></a>链表结构体</h2><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">list_head</span> &#123;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">list_head</span> *<span class="title">prev</span>;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">list_head</span> *<span class="title">next</span>;</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>非常的简单，结构体中一共就两个成员，分别是<strong>指向上一个链表节点的指针</strong>、<strong>指向下一个链表节点的指针</strong>.<br>这让我想起来，<code>Linux</code>内核中链表的实现好像也是这样的，但是当时的我太菜了，没有继续去了解。</p>
<h3 id="如何使用呢？"><a href="#如何使用呢？" class="headerlink" title="如何使用呢？"></a>如何使用呢？</h3><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> NAME_SIZE 20</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> &#123;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">list_head</span> <span class="title">link</span>;</span>  <span class="comment">// 包含这个链表结构体</span></span><br><span class="line">    <span class="type">char</span> name[NAME_SIZE];</span><br><span class="line">    <span class="type">int</span> age;</span><br><span class="line">&#125; Person;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 初始化一个person</span></span><br><span class="line">Person * <span class="title function_">init_person</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *name, <span class="type">int</span> age)</span> &#123;</span><br><span class="line">    Person *p = (Person *)<span class="built_in">malloc</span>(<span class="keyword">sizeof</span>(Person));</span><br><span class="line">    <span class="built_in">strcpy</span>(p-&gt;name, name);</span><br><span class="line">    p-&gt;age = age;</span><br><span class="line">    <span class="keyword">return</span> p;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这里我们为了展示如何使用链表，建立了一个简单的结构体 <code>Person</code>，用来表示一个人。<br>可以看到，我们在结构体里面添加了成员 <code>struct list_head link;</code>，这样我们就可以依靠这个 <code>link</code> 成员来组成一个<code>person</code> 链表。</p>
<h2 id="链表头"><a href="#链表头" class="headerlink" title="链表头"></a>链表头</h2><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">static</span> <span class="keyword">inline</span> <span class="type">void</span> <span class="title function_">init_list_head</span><span class="params">(<span class="keyword">struct</span> list_head *head)</span></span><br><span class="line">&#123;</span><br><span class="line">    head-&gt;prev = head;</span><br><span class="line">    head-&gt;next = head;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">list_head</span> <span class="title">person_list</span>;</span></span><br><span class="line">    init_list_head(&amp;person_list);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>而一个链表，需要一个头节点，这个头节点是不存储数据的，在这里也就是头节点不会被包含在 <code>person</code> 的结构体成员中，而是直接定义一个 <code>struct list_head</code> 变量。<br>然后调用 <code>init_list_head</code> 函数来初始化它，这里初始化完毕之后，<code>person_list</code> 变量它的 <code>prev</code> 指针和<code>next</code> 指针均指向自己，这种状态可以用来表示这个链表目前是<strong>空</strong>的。</p>
<h2 id="向链表添加数据"><a href="#向链表添加数据" class="headerlink" title="向链表添加数据"></a>向链表添加数据</h2><p>现在我们向这个链表添加一个<code>person</code>，在添加之前呢，还有一些问题需要解决。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* insert &#x27;el&#x27; between &#x27;prev&#x27; and &#x27;next&#x27; */</span></span><br><span class="line"><span class="type">static</span> <span class="keyword">inline</span> <span class="type">void</span> __list_add(<span class="keyword">struct</span> list_head *el,</span><br><span class="line">                              <span class="keyword">struct</span> list_head *prev, <span class="keyword">struct</span> list_head *next)</span><br><span class="line">&#123;</span><br><span class="line">    prev-&gt;next = el;</span><br><span class="line">    el-&gt;prev = prev;</span><br><span class="line">    el-&gt;next = next;</span><br><span class="line">    next-&gt;prev = el;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>上面这个函数，通俗易懂，简而言之，<strong>在两个节点之间插入一个新的节点</strong>。</p>
<p>依靠 <code>__list_add</code> 函数，我们就可以实现下面两个功能：</p>
<ol>
<li><code>list_add</code>：在链表头部位置插入一个节点（注：在链表头的后一个位置插入，因为链表头是不保存数据的）。</li>
<li><code>list_add_tail</code>：在链表尾部位置插入一个节点。<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* add &#x27;el&#x27; at the head of the list &#x27;head&#x27; (= after element head) */</span></span><br><span class="line"><span class="type">static</span> <span class="keyword">inline</span> <span class="type">void</span> <span class="title function_">list_add</span><span class="params">(<span class="keyword">struct</span> list_head *el, <span class="keyword">struct</span> list_head *head)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// 在头节点，和头节点的下一个节点之间插入新的节点 el</span></span><br><span class="line">    __list_add(el, head, head-&gt;next);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* add &#x27;el&#x27; at the end of the list &#x27;head&#x27; (= before element head) */</span></span><br><span class="line"><span class="type">static</span> <span class="keyword">inline</span> <span class="type">void</span> <span class="title function_">list_add_tail</span><span class="params">(<span class="keyword">struct</span> list_head *el, <span class="keyword">struct</span> list_head *head)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// 在头节点的上一个节点，头节点之间插入新的节点 el</span></span><br><span class="line">    __list_add(el, head-&gt;prev, head);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">list_head</span> <span class="title">person_list</span>;</span></span><br><span class="line">    init_list_head(&amp;person_list);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 在person_list头部插入一个person</span></span><br><span class="line">    Person *p = init_person(<span class="string">&quot;dog&quot;</span>, <span class="number">23</span>);</span><br><span class="line">    list_add(&amp;p-&gt;link, &amp;person_list);</span><br><span class="line">    </span><br><span class="line">    Person *p1 = init_person(<span class="string">&quot;cat&quot;</span>, <span class="number">22</span>);</span><br><span class="line">    list_add(&amp;p1-&gt;link, &amp;person_list);</span><br><span class="line"></span><br><span class="line">    Person *p2 = init_person(<span class="string">&quot;pig&quot;</span>, <span class="number">77</span>);</span><br><span class="line">    list_add_tail(&amp;p2-&gt;link, &amp;person_list);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ol>
<p>大家是不是对<code>list_add_tail</code> 函数的实现感到奇怪？他明明实在头节点的上一个节点和头节点之间插入一个元素啊，为什么就插到链表的尾部去了呢？？？<br>其实你只要稍微想一想就会明白，这<code>tm</code>是一个循环链表啊！！！<br>头节点的上一个节点，就是链表的最后一个节点，这是一个循环！！！<br>接下来就很简单了。</p>
<h2 id="链表for循环"><a href="#链表for循环" class="headerlink" title="链表for循环"></a>链表for循环</h2><p><code>Fabrice Bellard</code> 贴心的封装了好几个宏，方便使用。我测试了前面两个宏。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> list_for_each(el, head) \</span></span><br><span class="line"><span class="meta">  for(el = (head)-&gt;next; el != (head); el = el-&gt;next)</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> list_for_each_safe(el, el1, head)                \</span></span><br><span class="line"><span class="meta">    for(el = (head)-&gt;next, el1 = el-&gt;next; el != (head); \</span></span><br><span class="line"><span class="meta">        el = el1, el1 = el-&gt;next)</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> list_for_each_prev(el, head) \</span></span><br><span class="line"><span class="meta">  for(el = (head)-&gt;prev; el != (head); el = el-&gt;prev)</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> list_for_each_prev_safe(el, el1, head)           \</span></span><br><span class="line"><span class="meta">    for(el = (head)-&gt;prev, el1 = el-&gt;prev; el != (head); \</span></span><br><span class="line"><span class="meta">        el = el1, el1 = el-&gt;prev)</span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">list_head</span> <span class="title">person_list</span>;</span></span><br><span class="line">    init_list_head(&amp;person_list);</span><br><span class="line"></span><br><span class="line">    Person *p = init_person(<span class="string">&quot;dog&quot;</span>, <span class="number">23</span>);</span><br><span class="line">    list_add(&amp;p-&gt;link, &amp;person_list);</span><br><span class="line">    </span><br><span class="line">    Person *p1 = init_person(<span class="string">&quot;cat&quot;</span>, <span class="number">22</span>);</span><br><span class="line">    list_add(&amp;p1-&gt;link, &amp;person_list);</span><br><span class="line"></span><br><span class="line">    Person *p2 = init_person(<span class="string">&quot;pig&quot;</span>, <span class="number">77</span>);</span><br><span class="line">    list_add_tail(&amp;p2-&gt;link, &amp;person_list);</span><br><span class="line"></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">list_head</span> *<span class="title">node</span>, *<span class="title">node1</span>;</span></span><br><span class="line">    list_for_each(node, &amp;person_list) &#123;</span><br><span class="line">        Person *tp = list_entry(node, Person, link);<span class="comment">// 下文会提到list_entry宏是啥</span></span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;name: %s, age: %d\n&quot;</span>, tp-&gt;name, tp-&gt;age);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    list_for_each_safe(node, node1, &amp;person_list) &#123;</span><br><span class="line">        list_del(node); <span class="comment">// 删除节点，后文也会提到</span></span><br><span class="line">        <span class="built_in">free</span>(node); <span class="comment">// 释放内存</span></span><br><span class="line">    &#125;</span><br><span class="line">    assert(person_list.next == person_list.prev);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>结果输出完全符合要求：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">name: <span class="built_in">cat</span>, age: 22</span><br><span class="line">name: dog, age: 23</span><br><span class="line">name: pig, age: 77</span><br></pre></td></tr></table></figure>

<p>这里还有一个宏没有交代：<code>list_entry</code>。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> CONTAINER_OF(ptr, type, member) \</span></span><br><span class="line"><span class="meta">    (type *)((char *)(ptr) - (char *) &amp;((type *)0)-&gt;member)</span></span><br><span class="line"></span><br><span class="line"><span class="comment">/* return the pointer of type &#x27;type *&#x27; containing &#x27;el&#x27; as field &#x27;member&#x27; */</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> list_entry(el, type, member) CONTAINER_OF(el, type, member)</span></span><br></pre></td></tr></table></figure>
<p>这个宏就是链表实现的精髓所在了。<br>还记得前面的Person结构体吗？我们在里面保存了链表结构体成员 <code>struct list_head link</code></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> &#123;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">list_head</span> <span class="title">link</span>;</span> <span class="comment">// 包含这个链表结构体</span></span><br><span class="line">    <span class="type">char</span> name[NAME_SIZE];</span><br><span class="line">    <span class="type">int</span> age;</span><br><span class="line">&#125; Person;</span><br></pre></td></tr></table></figure>
<p>而这个list_entry 宏的用法如下：<br><code>Person *tp = list_entry(node, Person, link);</code><br>我们把这个list_entry宏展开看一看是啥：<br><code>(Person *) ( (char *)(node) - (char *)&amp;((Person *)0)-&gt;link )</code><br>分开讲讲就是</p>
<ul>
<li><code>(char *)(node)</code>：link成员变量的实际地址。</li>
<li><code>(char *)&amp;((Person *)0)-&gt;link</code>：link成员的距离Person结构体首地址的偏移量。<br>把这两个指针相减，就是person变量的实际地址。</li>
</ul>
<h2 id="链表节点删除"><a href="#链表节点删除" class="headerlink" title="链表节点删除"></a>链表节点删除</h2><p>通俗易懂，链表无需管理内存释放，需要我们手动释放，很合理。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">static</span> <span class="keyword">inline</span> <span class="type">void</span> <span class="title function_">list_del</span><span class="params">(<span class="keyword">struct</span> list_head *el)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">list_head</span> *<span class="title">prev</span>, *<span class="title">next</span>;</span></span><br><span class="line">    prev = el-&gt;prev;</span><br><span class="line">    next = el-&gt;next;</span><br><span class="line">    prev-&gt;next = next;</span><br><span class="line">    next-&gt;prev = prev;</span><br><span class="line">    el-&gt;prev = <span class="literal">NULL</span>; <span class="comment">/* fail safe */</span></span><br><span class="line">    el-&gt;next = <span class="literal">NULL</span>; <span class="comment">/* fail safe */</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="链表清空"><a href="#链表清空" class="headerlink" title="链表清空"></a>链表清空</h2><p>太极客了，哈哈哈</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">static</span> <span class="keyword">inline</span> <span class="type">int</span> <span class="title function_">list_empty</span><span class="params">(<span class="keyword">struct</span> list_head *el)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">return</span> el-&gt;next == el;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>


<h2 id="源码实现"><a href="#源码实现" class="headerlink" title="源码实现"></a>源码实现</h2><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> * Linux klist like system</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * Copyright (c) 2016-2017 Fabrice Bellard</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * Permission is hereby granted, free of charge, to any person obtaining a copy</span></span><br><span class="line"><span class="comment"> * of this software and associated documentation files (the &quot;Software&quot;), to deal</span></span><br><span class="line"><span class="comment"> * in the Software without restriction, including without limitation the rights</span></span><br><span class="line"><span class="comment"> * to use, copy, modify, merge, publish, distribute, sublicense, and/or sell</span></span><br><span class="line"><span class="comment"> * copies of the Software, and to permit persons to whom the Software is</span></span><br><span class="line"><span class="comment"> * furnished to do so, subject to the following conditions:</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * The above copyright notice and this permission notice shall be included in</span></span><br><span class="line"><span class="comment"> * all copies or substantial portions of the Software.</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * THE SOFTWARE IS PROVIDED &quot;AS IS&quot;, WITHOUT WARRANTY OF ANY KIND, EXPRESS OR</span></span><br><span class="line"><span class="comment"> * IMPLIED, INCLUDING BUT NOT LIMITED TO THE WARRANTIES OF MERCHANTABILITY,</span></span><br><span class="line"><span class="comment"> * FITNESS FOR A PARTICULAR PURPOSE AND NONINFRINGEMENT. IN NO EVENT SHALL</span></span><br><span class="line"><span class="comment"> * THE AUTHORS OR COPYRIGHT HOLDERS BE LIABLE FOR ANY CLAIM, DAMAGES OR OTHER</span></span><br><span class="line"><span class="comment"> * LIABILITY, WHETHER IN AN ACTION OF CONTRACT, TORT OR OTHERWISE, ARISING FROM,</span></span><br><span class="line"><span class="comment"> * OUT OF OR IN CONNECTION WITH THE SOFTWARE OR THE USE OR OTHER DEALINGS IN</span></span><br><span class="line"><span class="comment"> * THE SOFTWARE.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="meta">#<span class="keyword">ifndef</span> LIST_H</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> LIST_H</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">ifndef</span> NULL</span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stddef.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">list_head</span> &#123;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">list_head</span> *<span class="title">prev</span>;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">list_head</span> *<span class="title">next</span>;</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> LIST_HEAD_INIT(el) &#123; &amp;(el), &amp;(el) &#125;</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> CONTAINER_OF(ptr, type, member) \</span></span><br><span class="line"><span class="meta">    (type *)((char *)(ptr) - (char *) &amp;((type *)0)-&gt;member)</span></span><br><span class="line"></span><br><span class="line"><span class="comment">/* return the pointer of type &#x27;type *&#x27; containing &#x27;el&#x27; as field &#x27;member&#x27; */</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> list_entry(el, type, member) CONTAINER_OF(el, type, member)</span></span><br><span class="line"></span><br><span class="line"><span class="type">static</span> <span class="keyword">inline</span> <span class="type">void</span> <span class="title function_">init_list_head</span><span class="params">(<span class="keyword">struct</span> list_head *head)</span></span><br><span class="line">&#123;</span><br><span class="line">    head-&gt;prev = head;</span><br><span class="line">    head-&gt;next = head;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* insert &#x27;el&#x27; between &#x27;prev&#x27; and &#x27;next&#x27; */</span></span><br><span class="line"><span class="type">static</span> <span class="keyword">inline</span> <span class="type">void</span> __list_add(<span class="keyword">struct</span> list_head *el,</span><br><span class="line">                              <span class="keyword">struct</span> list_head *prev, <span class="keyword">struct</span> list_head *next)</span><br><span class="line">&#123;</span><br><span class="line">    prev-&gt;next = el;</span><br><span class="line">    el-&gt;prev = prev;</span><br><span class="line">    el-&gt;next = next;</span><br><span class="line">    next-&gt;prev = el;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* add &#x27;el&#x27; at the head of the list &#x27;head&#x27; (= after element head) */</span></span><br><span class="line"><span class="type">static</span> <span class="keyword">inline</span> <span class="type">void</span> <span class="title function_">list_add</span><span class="params">(<span class="keyword">struct</span> list_head *el, <span class="keyword">struct</span> list_head *head)</span></span><br><span class="line">&#123;</span><br><span class="line">    __list_add(el, head, head-&gt;next);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* add &#x27;el&#x27; at the end of the list &#x27;head&#x27; (= before element head) */</span></span><br><span class="line"><span class="type">static</span> <span class="keyword">inline</span> <span class="type">void</span> <span class="title function_">list_add_tail</span><span class="params">(<span class="keyword">struct</span> list_head *el, <span class="keyword">struct</span> list_head *head)</span></span><br><span class="line">&#123;</span><br><span class="line">    __list_add(el, head-&gt;prev, head);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">static</span> <span class="keyword">inline</span> <span class="type">void</span> <span class="title function_">list_del</span><span class="params">(<span class="keyword">struct</span> list_head *el)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">list_head</span> *<span class="title">prev</span>, *<span class="title">next</span>;</span></span><br><span class="line">    prev = el-&gt;prev;</span><br><span class="line">    next = el-&gt;next;</span><br><span class="line">    prev-&gt;next = next;</span><br><span class="line">    next-&gt;prev = prev;</span><br><span class="line">    el-&gt;prev = <span class="literal">NULL</span>; <span class="comment">/* fail safe */</span></span><br><span class="line">    el-&gt;next = <span class="literal">NULL</span>; <span class="comment">/* fail safe */</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">static</span> <span class="keyword">inline</span> <span class="type">int</span> <span class="title function_">list_empty</span><span class="params">(<span class="keyword">struct</span> list_head *el)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">return</span> el-&gt;next == el;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> list_for_each(el, head) \</span></span><br><span class="line"><span class="meta">  for(el = (head)-&gt;next; el != (head); el = el-&gt;next)</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> list_for_each_safe(el, el1, head)                \</span></span><br><span class="line"><span class="meta">    for(el = (head)-&gt;next, el1 = el-&gt;next; el != (head); \</span></span><br><span class="line"><span class="meta">        el = el1, el1 = el-&gt;next)</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> list_for_each_prev(el, head) \</span></span><br><span class="line"><span class="meta">  for(el = (head)-&gt;prev; el != (head); el = el-&gt;prev)</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> list_for_each_prev_safe(el, el1, head)           \</span></span><br><span class="line"><span class="meta">    for(el = (head)-&gt;prev, el1 = el-&gt;prev; el != (head); \</span></span><br><span class="line"><span class="meta">        el = el1, el1 = el-&gt;prev)</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">endif</span> <span class="comment">/* LIST_H */</span></span></span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>编程语言</category>
        <category>C语言</category>
        <category>数据结构</category>
        <category>链表</category>
      </categories>
      <tags>
        <tag>C</tag>
        <tag>链表</tag>
        <tag>双向链表</tag>
        <tag>quickjs</tag>
      </tags>
  </entry>
  <entry>
    <title>Hello World</title>
    <url>/2024/04/13/hello-world/</url>
    <content><![CDATA[<p>Welcome to <a href="https://hexo.io/">Hexo</a>! This is your very first post. Check <a href="https://hexo.io/docs/">documentation</a> for more info. If you get any problems when using Hexo, you can find the answer in <a href="https://hexo.io/docs/troubleshooting.html">troubleshooting</a> or you can ask me on <a href="https://github.com/hexojs/hexo/issues">GitHub</a>.</p>
<h2 id="Quick-Start"><a href="#Quick-Start" class="headerlink" title="Quick Start"></a>Quick Start</h2><h3 id="Create-a-new-post"><a href="#Create-a-new-post" class="headerlink" title="Create a new post"></a>Create a new post</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo new <span class="string">&quot;My New Post&quot;</span></span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/writing.html">Writing</a></p>
<h3 id="Run-server"><a href="#Run-server" class="headerlink" title="Run server"></a>Run server</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo server</span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/server.html">Server</a></p>
<h3 id="Generate-static-files"><a href="#Generate-static-files" class="headerlink" title="Generate static files"></a>Generate static files</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo generate</span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/generating.html">Generating</a></p>
<h3 id="Deploy-to-remote-sites"><a href="#Deploy-to-remote-sites" class="headerlink" title="Deploy to remote sites"></a>Deploy to remote sites</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo deploy</span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/one-command-deployment.html">Deployment</a></p>
]]></content>
  </entry>
  <entry>
    <title>第一篇post</title>
    <url>/2024/04/13/%E7%AC%AC%E4%B8%80%E7%AF%87post/</url>
    <content><![CDATA[<p>这是一个没用的post<br>$a_1$</p>
]]></content>
      <categories>
        <category>日常</category>
      </categories>
      <tags>
        <tag>废话</tag>
      </tags>
  </entry>
  <entry>
    <title>2025新的一年</title>
    <url>/2024/12/31/2025%E6%96%B0%E7%9A%84%E4%B8%80%E5%B9%B4/</url>
    <content><![CDATA[<p>今天是2024年最后一天，故此让我的杂货铺开一下张。</p>
<span id="more"></span>

<p>没啥好说的，元旦后还要组会，汇报课题进展，挺忙的。<br>这里希望自己明年能够发篇论文，早点毕业。</p>
<p>然后今年我的<code>cs</code>水平突飞猛进，现在已经来到260个小时。</p>
<p>还有啥？吃好喝好，别太拼了。</p>
]]></content>
      <categories>
        <category>日常</category>
      </categories>
      <tags>
        <tag>废话</tag>
      </tags>
  </entry>
</search>
